// A grid of glyph buttons — the "pick an icon" surface for a namespace.
// The implementation lives in @niclaslindstedt/oss-framework (its
// `GlyphPicker`, whose leading cell clears back to the default glyph); this
// shim keeps the app's historical import path and name.
export { GlyphPicker as GlyphGrid } from "@niclaslindstedt/oss-framework/glyphs";
