// Portalled dropdown / popover shell (float position, Escape +
// outside-click dismissal). The implementation lives in
// @niclaslindstedt/oss-framework; this shim keeps the app's historical
// import path.
export { FloatingPanel } from "@niclaslindstedt/oss-framework/components";
