// Pins a `position: fixed` full-screen overlay over the same band as the
// app shell. The implementation lives in @niclaslindstedt/oss-framework;
// this shim keeps the app's historical import path.
export { APP_VIEWPORT_RECT } from "@niclaslindstedt/oss-framework/components";
