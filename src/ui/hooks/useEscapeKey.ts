// Capture-phase Escape handler, gated on an `enabled` flag. The
// implementation lives in @niclaslindstedt/oss-framework; this shim keeps
// the app's historical import path.
export { useEscapeKey } from "@niclaslindstedt/oss-framework/hooks";
