---
type: Added
title: Shorten links
---

The live preview can now trim long pasted URLs down to the domain, a few characters either side of a `[...]` marker — toggle it and pick the length in Settings → Editor (the full link is still saved and still opens).
