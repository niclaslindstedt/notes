---
type: Added
title: Desktop app for Windows, macOS, and Linux
---

Every release now ships downloadable desktop builds — Windows, Linux, and macOS
for both Intel and Apple Silicon — that run the same app in its own window
instead of a browser tab.
