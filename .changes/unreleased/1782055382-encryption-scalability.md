---
type: Changed
title: Faster encryption at scale
---

Encrypted folder/cloud vaults with hundreds of notes now unlock far faster (notes decrypt in parallel instead of one at a time) and stay snappy while editing (each save re-encrypts only the note you changed, not the whole vault).
