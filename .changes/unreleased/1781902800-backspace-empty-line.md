---
type: Fixed
title: Holding Backspace keeps erasing across lines
---

Holding Backspace in the Markdown editor now keeps deleting onto the previous line instead of getting stuck at the start of an emptied line, so a held Backspace erases continuously the way it does everywhere else.
