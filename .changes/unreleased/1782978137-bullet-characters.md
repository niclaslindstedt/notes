---
type: Changed
title: Clearer nested-list bullets
---

Nested bulleted lists in the live preview now draw `•`, `-`, and `+` by level, using characters the app font renders so the markers stay crisp and centred on every device.
