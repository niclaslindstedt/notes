---
type: Changed
title: Cloud glyph moved to the side menu
---

The cloud sync glyph has moved out of every screen's top bar into the side
menu's button island, right of the search button, so it is one control in one
place instead of one per screen.
