---
type: Changed
title: The macOS download is signed and notarized
---

The macOS app is now signed with a Developer ID certificate and notarized by Apple, so it opens like any other app instead of being refused on first launch until it is allowed in System Settings.
