---
type: Added
title: Each note has its own title field
---

A note's title is now a dedicated field at the top of the editor — typed there rather than as the first body line, rendered like a heading, and stored as `title:` in the markdown frontmatter; existing notes have their first line lifted into it automatically.
