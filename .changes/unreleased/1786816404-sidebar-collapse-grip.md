---
type: Changed
title: A collapsed sidebar now gives back all of its space
---

Folding the desktop sidebar away leaves no gutter behind — the note takes the
whole window, and the handle that brings the sidebar back appears only when you
move the pointer to that edge of the screen (it also lets the row highlight run
the sidebar's full width while it's open).
