---
type: Fixed
title: Note title matches the body font
---

The note title field now inherits the editor's font family and left alignment instead of falling back to the browser's default control font, so it reads as a true H1 heading of the note rather than a mismatched form input.
