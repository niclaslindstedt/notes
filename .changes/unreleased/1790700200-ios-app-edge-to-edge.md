---
type: Fixed
title: The iOS app fills the screen edge to edge, like the installed PWA
---

The App Store build framed the page inside the safe area, leaving a flat band above it, so the side menu and the note headers stopped short of the status bar. The page now runs full-bleed on iOS and pads itself around the notch and home indicator exactly as the PWA does.
