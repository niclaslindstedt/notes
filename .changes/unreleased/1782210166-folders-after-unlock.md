---
type: Fixed
title: Folders after unlocking on a cloud backend
---

The folder registry is now read directly from its sidecar rather than trusting the directory listing, so folders (including empty ones) show immediately after unlocking encryption on app start instead of only after switching namespaces.
