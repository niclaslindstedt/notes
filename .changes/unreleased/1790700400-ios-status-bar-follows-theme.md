---
type: Fixed
title: The iOS status bar stays legible on every theme
---

The status bar took its colour from the phone's light or dark setting rather than the app's theme, so with the phone in light mode its clock and icons were drawn dark over the default dark theme — invisible now that the page runs under the status bar. The status bar and the background behind the page now follow the theme the app shows, and switch with it.
